name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node: [18.x]
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - name: Install dependencies
        run: npm install
      - name: List node_modules after install
        run: |
          if [ -d node_modules ]; then ls -l node_modules; else echo 'No node_modules directory found.'; fi
      - name: Link ppm CLI
        run: npm link
      - name: Ensure bin/ppm.js is executable
        run: chmod +x bin/ppm.js
      - name: Add local bin to PATH
        run: echo "$(pwd)/node_modules/.bin" >> $GITHUB_PATH
      - name: Test install command
        run: node bin/ppm.js install lodash
      - name: Test uninstall command
        run: node bin/ppm.js uninstall lodash
      - name: Test list command
        run: node bin/ppm.js list
      - name: Test script support
        run: |
          echo '{"scripts":{"test":"echo test-script"}}' > package.json
          node bin/ppm.js run test
      - name: Test config file support
        run: |
          echo '{"customSettings":{"cacheDirectory":".ppm-cache"}}' > ppm.json
          node bin/ppm.js install lodash
      - name: Test cache reuse
        run: |
          rm -rf node_modules
          node bin/ppm.js install lodash
      - name: Test update/upgrade
        run: |
          node bin/ppm.js install lodash@4.17.20
          node bin/ppm.js update lodash
          node bin/ppm.js upgrade
      - name: List node_modules for debugging
        run: |
          if [ -d node_modules ]; then ls -l node_modules; else echo 'No node_modules directory found.'; fi
